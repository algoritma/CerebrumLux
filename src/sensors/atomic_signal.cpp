#include "atomic_signal.h"
// Bu cpp dosyasında AtomicSignal struct'ına özel başka bir fonksiyon olmadığı için,
// içeriği sadece include'lar ve namespace bloğu ile bırakıyoruz.
// Constructor zaten header dosyasında tanımlı (in-class initializer).

//ÖNEMLİ NOT: src/sensors/atomic_signal.cpp dosyasının içeriği, AtomicSignal struct'ının constructor'ı atomic_signal.h dosyasında zaten tanımlandığı için boş kalmalıdır. 

namespace CerebrumLux {

// Eğer AtomicSignal için ekstra metotlar varsa, onlar buraya gelecek.
// Şu an için yok.

} // namespace CerebrumLux
